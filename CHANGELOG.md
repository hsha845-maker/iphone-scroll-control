# 更新记录

## 1.2.5 — 2026-09-05

- 优化复杂视频背景下的首页推荐页识别；即使字幕遮挡“推荐 / For You”，仍可结合底部首页和右侧视频操作栏安全确认视频流。
- 备用规则要求至少三个纵向分离的右侧数字计数，避免在搜索、聊天等输入页面误启用快捷键。
- 新增页面识别回归检查，覆盖抖音与 TikTok 两种首页标签。

## 1.2.4 — 2026-09-05

- 修复首次切回镜像后，工具的自动聚焦点击被误算为用户播放/暂停操作，导致内部播放状态与视频实际状态相反的问题。
- 修复实际播放时按 Command + H 可能直接隐藏而没有暂停，以及实际暂停时隐藏反而重新播放的问题。
- 增加合成聚焦点击的事件标记回归检查，避免工具自身点击再次污染播放状态。
- 分享按钮不再只使用固定纵向比例：从右侧操作栏最下方的分享计数动态推算按钮位置，兼容带额外推荐/合集信息、操作栏整体下移的视频。
- 左方向键点赞仍使用视频中央双击，不依赖右侧爱心按钮位置。
- 登录启动项改为通过 LaunchServices 后台打开应用包，避免 launchd 直接运行包内二进制时被 macOS 识别为不同的隐私权限身份。

## 1.2.3 — 2026-09-05

- 已实机确认：同一进程内直接保留并回放原生触控板事件时，↓ 和 ↑ 都能完整切换 TikTok 视频。
- 停止在正式运行中加载磁盘反序列化的手势事件。虽然可检查的字段一致，重启恢复后的事件只会移动少量画面，无法让 TikTok 提交换页。
- 每次工具启动后需在 Home → For You 用触控板完整切到下一条、再切回上一条；完成本次运行的双向校准后，键盘方向键和鼠标侧键持续可用。
- 校准文件继续作为本机诊断记录保存，但不再误当作跨进程可用的原生事件。

## 1.2.2 — 2026-09-05（本机验证中）

- TikTok 同时采集并重放滚动事件与原生配套手势事件；临时完整手势诊断中，用户已验证 ↓ 可以连续切换下一条。
- ↑ 不再使用负号反转的推算手势：两个方向分别校准、保存和读取。仅反转公开位移字段未通过本机上一条验证，已移除该回退。
- 使用 v4 本地校准文件，保留旧文件，不把缺少配套事件的旧模板当成有效模板。重启加载完整序列；系统版本变化时重新校准。
- 按绝对时间发送手势，避免每帧处理开销累积拉长时长；同时最多执行一次翻页，不堆积按键。
- 指针在窗口外时只定位一次；用户移动指针或切走应用立即结束重放，不持续拉回指针。
- 滚动事件不再落入侧键处理分支；窗口位置缓存取代每个滚动事件同步查询 AX。
- 真实镜像处于前台时撤下浮动预览层；后台仍保留浮动预览，隐藏时继续停用。
- 新增只读事件档案检查与实际校准类回归测试。完整翻页效果仍须验证，不能由编译或重放日志推断成功。

## 1.2.1 — 2026-09-05

- TikTok 改用本机一次性触控板校准：分别采集下一条和上一条的真实滚动事件，之后完整重放；抖音仍使用原有滚轮参数。
- 校准仅保存滚动事件，不采集键盘或画面；保存于本机 Application Support，系统版本变化时要求重新校准。
- TikTok 视频页的普通鼠标滚轮转换为翻页手势，连续滚动采用 650ms 静默间隔防重复；触控板及其他页面不转换。
- 英文 OCR 兼容 `For You` / `ForYou` 等空格差异。
- 本机重新绑定新版的辅助功能、输入监控和屏幕录制权限，已恢复方向键事件接收。
- 编译与静态检查通过；实际翻页效果待本机验证。

## 1.2.0 — 2026-09-05

- 增加 TikTok 英文版 Home / For You 视频页及 Send to / Share to 分享面板识别。
- TikTok 使用独立的分享按钮及六列联系人布局，1–5 对应当前行前五项。
- 英文发送按钮通过 OCR 定位；没有识别到 Send 时不点击固定坐标。
- 分享留言输入也遵守键盘输入保护期。
- 已核对英文视频页和初始分享面板；联系人选择后的发送流程需要用户实测。

## 1.1.9 — 2026-09-04

- 修复选择联系人后分享面板变为简化布局，导致 `Enter` 无法发送的问题。
- 新增“分享给 + 发送”布局识别，并为分享面板增加 5 秒识别容错。
- 鼠标手动选择联系人时保留分享状态，随后也可按 `Enter` 发送。

## 1.1.8 — 2026-09-02

- 修复首页“推荐”视频受弹幕或动态字幕影响时，OCR 偶发漏识别导致方向键失效并发出系统提示音的问题。
- 键盘快捷键和鼠标侧键现在允许使用短暂的“最近已确认推荐页”状态；进入搜索、聊天、分享或开始输入时仍会立即停用，避免干扰打字。

## 1.1.7 — 2026-09-02

- 修复 OCR 短暂漏识别视频页时，隐藏或最小化窗口可能跳过暂停、导致声音继续播放的问题。
- 窗口操作允许使用 3 秒内最近一次可靠的视频页识别结果；点击页面或开始打字会立即取消该结果，避免在搜索和聊天页误触。
- 暂停点击与隐藏/最小化之间的等待时间由 0.12 秒延长到 0.35 秒，确保抖音完成暂停。

## 1.1.6 — 2026-09-02

- 修复中文输入法按空格确认候选词时，空格被错误识别为播放/暂停快捷键的问题。
- “首页”和“推荐”必须分别出现在底部导航与顶部标签的正确位置，搜索结果中的同名文字不再导致页面误判。
- 增加 2 秒文本输入保护期，打字期间的空格、方向键和数字键完整交给输入法。
- 丢弃在点击或打字前启动、之后才返回的过期 OCR 结果，避免快捷键状态被异步恢复。

## 1.1.5 — 2026-09-02

- 修复鼠标手动选中联系人后，分享弹窗中按 `Enter` 没有反应的问题。
- 分享弹窗显示时，按右方向键关闭弹窗。
- 跟踪视频播放/暂停状态；视频已暂停时隐藏或最小化窗口不再点击视频，避免意外恢复播放。

## 1.1.4 — 2026-09-02

- 仅在 OCR 同时识别到“首页”和“推荐”时启用视频快捷键。
- 搜索、聊天及其他页面完整放行键盘输入，点击页面或开始打字时立即暂停快捷键。
- 新增独立的分享弹窗识别；仅在“分享给”弹窗可见时启用 `1`–`5` 选人和 `Enter` 发送。

## 1.1.3 — 2026-09-01

- iPhone Mirroring 隐藏、最小化或离开当前桌面时真正停止 ScreenCaptureKit 采集。
- 停止采集后允许 macOS 菜单栏的 sharing 指示自动结束。
- 镜像窗口恢复可见后自动创建新的采集流并恢复悬浮预览。

## 1.1.2 — 2026-08-31

- 修复 iPhone Mirroring 隐藏后，点击原窗口区域会意外重新唤出窗口的问题。
- 隐藏或最小化开始时立即撤下悬浮预览，不再等待定时刷新。
- 增加隐藏、最小化和离开当前桌面的可交互状态校验，暂停全部镜像快捷操作。
- 将窗口状态检测间隔由 0.5 秒缩短至 0.1 秒。

## 1.1.1 — 2026-08-30

- 分享联系人快捷键从 `1`–`3` 扩展到 `1`–`5`。
- 同时支持主键盘数字区与数字小键盘。
- 分享状态下优先处理联系人数字键，避免小键盘别名冲突。

## 1.1.0 — 2026-08-30

- 增加实时悬浮预览与点击激活真实 iPhone Mirroring。
- 修复真实窗口最小化或隐藏后预览仍遮挡其他窗口的问题。
- 点击黄色按钮、按 `Command + M` 或 `Command + H` 时先暂停视频。
- 优化上下独立圆角遮罩，移除四角黑边与面板投影。
- 增加上、下、左、右、空格及分享相关键盘快捷键。
- 通过 Vision 区分视频页、聊天页与分享面板，保护聊天输入。
- 增加自动重复抑制、去抖和首次焦点恢复。
- 提供 Apple Silicon 与 Intel 通用二进制。

## 1.0.0 — 2026-08-30

- 使用 CGEventTap 监听 Logitech 鼠标侧键。
- 后侧键切换下一条，前侧键切换上一条。
- 仅在 iPhone Mirroring 前台时拦截侧键。
- 使用滚轮事件模拟抖音翻页。
- 提供登录自动启动、日志与权限提示。
